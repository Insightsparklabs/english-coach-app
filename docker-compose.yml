version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      # ホスト(あなたのPC)の8000番ポートを、コンテナの8080番に繋ぐ
      - "8000:8080"
    volumes:
      # コードを書き換えたら即材に反映されるように同期する(ホットリロード)
      - ./backend:/app
    # 環境変数は後で .env ファイルから読み込む設定
    env_file:
      - .env

  frontend:
    build: ./frontend
    ports:
      - "8501:8501"
    env_file:
      - .env  # 👈 【超重要】フロントエンドにもこれを追加！！！
    environment:
      - BACKEND_BASE_URL=http://backend:8080  # 👈 【ここを追加！】Docker内での連絡先を上書きします
    volumes:
      - ./frontend:/app
    depends_on:
      - backend
      
